version: '3.9'
services:
  kibana:
    image: docker.elastic.co/kibana/kibana:8.15.0
    container_name: kibana
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://3.35.226.34:9200
    networks:
      - homesweet-network

networks:
  homesweet-network:
    driver: bridge